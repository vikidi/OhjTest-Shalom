import isBuffer from '../src/isBuffer';

/* Documentation Notes
 * 
 * 
 */

describe('isBuffer()', () => {
  describe('positive tests', () => {
    test.todo('')
  })

  describe('negative tests', () => {
    test.todo('')
  })
})