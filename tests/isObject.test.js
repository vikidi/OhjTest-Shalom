import isObject from '../src/isObject';

/* Documentation Notes
 * 
 * 
 */

describe('isObject()', () => {
  describe('positive tests', () => {
    test.todo('')
  })

  describe('negative tests', () => {
    test.todo('')
  })
})